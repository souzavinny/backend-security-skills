# Findings

Audit reports are written here by the `nodejs-api-auditor` skill:

```
{project-name}-api-audit-report-{YYYYMMDD-HHMMSS}.md
```

Individual report files are git-ignored. Keep this README committed so the
directory exists.
