# Findings

Runtime audit reports are written here by the `runtime-audit` skill:

```
{target-host}-runtime-audit-{YYYYMMDD-HHMMSS}.md
```

Individual report files are git-ignored. Keep this README committed so the directory exists.
